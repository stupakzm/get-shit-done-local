---
phase: 02-routing-and-assignment
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/set-model.test.cjs
autonomous: true
requirements:
  - EXEC-02
  - CMD-03
  - CMD-04

must_haves:
  truths:
    - "tests/set-model.test.cjs exists with failing stubs for config-set-model-override, reset, resolve passthrough, and previousValue diff output"
    - "Running the test file fails RED on all stubs — verifying the stubs name the right behaviors before implementation"
  artifacts:
    - path: "tests/set-model.test.cjs"
      provides: "Failing test scaffold for EXEC-02, CMD-03, CMD-04"
      contains: "config-set-model-override"
  key_links:
    - from: "tests/set-model.test.cjs"
      to: "tests/helpers.cjs"
      via: "require('./helpers.cjs')"
      pattern: "createTempProject|runGsdTools"
---

<objective>
Create the failing test scaffold for Phase 2 tooling before any implementation exists.

Purpose: Establish the RED baseline — all tests must fail with "command not found" or missing-file errors, proving they name real behaviors and aren't trivially green.
Output: tests/set-model.test.cjs with 6 failing stubs covering EXEC-02, CMD-03, CMD-04.
</objective>

<execution_context>
@C:/Users/TopAide/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TopAide/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routing-and-assignment/02-CONTEXT.md
@.planning/phases/02-routing-and-assignment/02-RESEARCH.md
</context>

<feature>
  <name>config-set-model-override test scaffold</name>
  <files>tests/set-model.test.cjs</files>
  <behavior>
    - Test 1 (EXEC-02): `gsd-tools config-set-model-override gsd-planner ollama:qwen2.5:7b` writes `model_overrides.gsd-planner = "ollama:qwen2.5:7b"` to config.json
    - Test 2 (EXEC-02): `gsd-tools config-set-model-override gsd-executor reset` deletes `model_overrides.gsd-executor` key from config.json entirely
    - Test 3 (EXEC-02): After override, `gsd-tools resolve-model gsd-planner` returns `ollama:qwen2.5:7b`
    - Test 4 (EXEC-02): After reset, `gsd-tools resolve-model gsd-planner` returns the profile default (not `ollama:*`)
    - Test 5 (CMD-03): `gsd-tools config-get model_overrides` returns the current overrides object (or empty object when none set)
    - Test 6 (CMD-04): `gsd-tools config-set-model-override gsd-planner sonnet --raw` returns JSON with `previousValue` field reflecting the prior assignment
  </behavior>
  <implementation>
    Use the existing test helper pattern from tests/helpers.cjs:
    - Import `createTempProject` and `runGsdTools` from `./helpers.cjs`
    - Each test calls `createTempProject()` to get an isolated tmpDir with a fresh .planning/config.json
    - Run gsd-tools commands via `runGsdTools(tmpDir, [...args])` — this is the established integration test pattern from tests/ollama.test.cjs and tests/config.test.cjs
    - `resolve-model` command already exists in gsd-tools.cjs (used in audit-milestone.md) — test against it directly

    RED phase: These tests MUST fail before plan 02 implements the command. Confirm failures are "Unknown command: config-set-model-override" (exit non-zero), not test infrastructure errors.

    After writing tests and confirming RED:
    - Commit: `test(02-01): add failing stubs for config-set-model-override and model-override resolve`
  </implementation>
</feature>

<verification>
`node --test tests/set-model.test.cjs` — all 6 tests fail RED (non-zero exit, "Unknown command" error in output, not infrastructure errors).
</verification>

<success_criteria>
6 test stubs exist, all fail RED on the `config-set-model-override` missing command. Test infrastructure itself (imports, createTempProject, runGsdTools) works correctly — only the feature-under-test is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-and-assignment/02-01-SUMMARY.md`
</output>
